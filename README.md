# DP-PPM
Dirichlet Process Product Partition Model

This repository contains the code to replicate the results in Chapter 4 of the thesis. The file src contains the raw Python code to run each model considered (including the DP-PPM). The notebook "plots.ipynb" contains instructions on how to reproduce the tables and figures.
